Perfect — yes 👍 let’s break your master prompt into **clear, digestible chunks** (each ~short enough for Copilot or Claude Sonnet 4.5 to fully handle).

You’ll feed them **one by one, in this exact order** — it will build the entire hybrid PyTorch pipeline step-by-step.

---

## 🧩 **Prompt 1 — Project setup & data loading**

> **Prompt 1:**
>
> Create a Python project structure for a hybrid exoplanet detection pipeline using PyTorch.
> The folder structure should look like this:
>
> ```
> exoplanet_hybrid/
> ├── data/
> │   ├── raw/
> │   ├── processed/
> │   │   ├── residual_windows/
> │   │   └── pixel_diffs/
> ├── src/
> │   ├── data_loader.py
> │   ├── features.py
> │   ├── pixel_diff.py
> │   ├── models.py
> │   ├── train.py
> │   └── evaluate.py
> ├── notebooks/
> │   └── demo_pipeline.ipynb
> ├── models/
> └── README.md
> ```
>
> In `src/data_loader.py`, write functions to:
>
> * Load and merge `all_global.csv` + `all_local.csv` on `kepid`.
> * Join with the cumulative KOI file (`q1_q17_dr25_sup_koi_2024.csv`) to get labels (`CONFIRMED`, `CANDIDATE`, `FALSE POSITIVE`).
> * Split data into train/validation/test using `GroupKFold` by `kepid`.
> * Return Pandas DataFrames and NumPy arrays ready for model input.
>
> Add any imports and docstrings you think necessary.

---

## ⚙️ **Prompt 2 — Feature extraction and preprocessing**

> **Prompt 2:**
>
> In `src/features.py`, implement preprocessing functions that:
>
> * Use the `lightkurve` library to download PDCSAP flux for a given `kepid`.
> * Detrend and normalize the light curve (e.g., with Savitzky–Golay or median filtering).
> * Run a **Box Least Squares (BLS)** search to find period, epoch, and duration.
> * Build a **phase-folded residual window** as a NumPy array `[2, L]` (residuals + smoothed trend) and save it as `.npy`.
> * Save residual windows under `data/processed/residual_windows/`.
>
> Also add a helper that loops through a list of `kepid` values to build these files in batch.

---

## 🌌 **Prompt 3 — Pixel difference generation**

> **Prompt 3:**
>
> In `src/pixel_diff.py`, write functions that:
>
> * Download the Target Pixel File (TPF) for a `kepid` using Lightkurve.
> * For the same period and epoch, stack in-transit vs out-of-transit frames.
> * Compute `(in_transit - out_transit) / std(out_transit)` as a normalized 2D array `[1, H, W]`.
> * Save results in `data/processed/pixel_diffs/`.
>
> Include batch processing for multiple KOIs.

---

## 🧠 **Prompt 4 — Define the PyTorch models**

> **Prompt 4:**
>
> In `src/models.py`, define the following PyTorch models:
>
> 1. **TabularNet (MLP)**
>
>    * Input: tabular features from `all_global + all_local`
>    * Architecture: 2–3 linear layers with ReLU + dropout, ending with sigmoid output.
> 2. **ResidualCNN1D**
>
>    * Input: `[2, 512]` residual windows.
>    * Architecture: Conv1D → ReLU → MaxPool → Conv1D → Flatten → Linear → Sigmoid.
> 3. **PixelCNN2D**
>
>    * Input: `[1, 16, 16]` pixel difference images.
>    * Architecture: Conv2D → ReLU → MaxPool → Flatten → Linear → Sigmoid.
>
> Each model should output a single probability (`p_tab`, `p_res`, `p_pix`).

---

## ⚡ **Prompt 5 — Training orchestration and stacker**

> **Prompt 5:**
>
> In `src/train.py`, create training and evaluation loops that:
>
> * Train each model (TabularNet, ResidualCNN1D, PixelCNN2D) separately using BCE loss and Adam optimizer.
> * Save best checkpoints to `models/` using torch.save().
> * After training, collect predictions (`p_tab`, `p_res`, `p_pix`) on the validation set.
> * Train a **late-fusion stacker** (small logistic regression or XGBoost) on those 3 probabilities.
> * Compute and print **ROC-AUC**, **PR-AUC**, and **Recall@1%FPR** for each head and for the final stacker.

---

## 📊 **Prompt 6 — Evaluation and visualization**

> **Prompt 6:**
>
> In `src/evaluate.py`, write helper functions that:
>
> * Plot ROC and Precision-Recall curves for all three heads and the stacker.
> * Generate confusion matrices and classification reports.
> * Save all metrics to `results/metrics.json`.
>
> Add matplotlib visualizations that summarize head vs fusion performance.

---

## 🚀 **Prompt 7 — Demo notebook**

> **Prompt 7:**
>
> Create `notebooks/demo_pipeline.ipynb` that:
>
> * Imports the modules from `src/`
> * Loads 10 example KOIs from the dataset
> * Builds one residual window and one pixel diff
> * Runs all three trained models + the fusion stacker
> * Displays the predicted probabilities and confidence plots
>
> Keep it runnable end-to-end as a demonstration of the full pipeline.

---

## 📘 **Prompt 8 — README.md and requirements**

> **Prompt 8:**
>
> Generate a `README.md` that explains:
>
> * The overall hybrid (XGBoost + CNN + Fusion) design
> * Folder structure
> * How to run preprocessing, training, and evaluation
> * Dependencies (`pandas`, `numpy`, `scipy`, `matplotlib`, `torch`, `lightkurve`, `xgboost`, `lightgbm`, `sklearn`, `tqdm`)
> * Example command:
>
>   ```bash
>   python src/train.py --epochs 10 --batch-size 32
>   ```

---

✅ **How to run them:**

1. Copy each prompt one at a time into Copilot.
2. Wait for it to generate each corresponding file/module.
3. After all are done, open `train.py` and test it on 100 KOIs (you can tweak later for full runs).

---

Would you like me to write **one final “refinement” prompt** (Prompt 9) that tells Copilot to improve the CNN layers and optimizers once the basic structure is done? (That’s usually where you can jump from 82–83% to 88–90% accuracy.)
